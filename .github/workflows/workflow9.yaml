name: Always Fails

on: [push, workflow_dispatch]

jobs:
  fail_job:
    runs-on: ubuntu-latest
    steps:
      - name: Fail intentionally
        run: |
          echo "🔴 This workflow is designed to always fail."
          exit 1
